import 'package:flutter/material.dart';

class SaloonContact extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFF0d1137),
        title: Text('Contact Us'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Contact Us'),
      ),
    );
  }
}

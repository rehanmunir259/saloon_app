import 'package:flutter/material.dart';

class SaloonFAQs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFF0d1137),
        title: Text('SaloonFAQs'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('SaloonFAQs'),
      ),
    );
  }
}
